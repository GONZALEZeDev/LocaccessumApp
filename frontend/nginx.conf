server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback: any path that isn't a real file resolves to index.html,
  # so client-side routes (e.g. a hard refresh on /profile) don't 404.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
